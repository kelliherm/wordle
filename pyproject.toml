[project]
name = "wordle"
version = "0.0.0"
description = "A wordle game in Python with a computer solver."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "colorama>=0.4.6",
    "numpy>=2.3.3",
]
